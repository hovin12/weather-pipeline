
services:

  init-dirs:
    image: alpine
    user: root
    volumes:
      - ./logs:/opt/airflow/logs
      - ./dags:/opt/airflow/dags
      - ./plugins:/opt/airflow/plugins
      - ./data:/opt/airflow/data
    command: sh -c "chown -R ${UID:-50000}:0 /opt/airflow/logs /opt/airflow/dags /opt/airflow/plugins /opt/airflow/data"

  postgres:
    image: postgres:13
    env_file:
      - .env
    volumes:
      - postgres-db:/var/lib/postgresql/data
      - ./src/api/mocks:/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: [ "CMD", "pg_isready", "-U", "airflow" ]
      interval: 5s
      retries: 10
    restart: unless-stopped

  airflow-webserver:
    build: .
    image: weather-airflow:2.8.1
    user: "${UID:-50000}:0"
    depends_on:
      postgres:
        condition: service_healthy
      init-dirs:
        condition: service_completed_successfully
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./dags:/opt/airflow/dags
      - ./logs:/opt/airflow/logs
      - ./plugins:/opt/airflow/plugins
      - ./data:/opt/airflow/data
      - ./src:/opt/airflow/src
      - ./requirements.txt:/requirements.txt
    command: ["webserver"]
    restart: unless-stopped

  airflow-scheduler:
    image: weather-airflow:2.8.1
    user: "${UID:-50000}:0"
    depends_on:
      postgres:
        condition: service_healthy
      init-dirs:
        condition: service_completed_successfully
    env_file:
      - .env
    volumes:
      - ./dags:/opt/airflow/dags
      - ./logs:/opt/airflow/logs
      - ./plugins:/opt/airflow/plugins
      - ./data:/opt/airflow/data
      - ./src:/opt/airflow/src
      - ./requirements.txt:/requirements.txt

    command: ["scheduler"]
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./data/html_output:/usr/share/nginx/html:ro
    restart: unless-stopped

volumes:
  postgres-db:
